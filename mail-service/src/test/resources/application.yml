spring:
  rabbitmq:
    queue: mail-service.mail.send
    exchange: mail-service.mail.ex
    routing-key: mail.send
  mail:
    host: 127.0.0.1
    port: 3025
    protocol: smtp
    username: fakemail
    password: password